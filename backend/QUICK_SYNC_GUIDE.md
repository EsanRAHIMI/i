# 🚀 راهنمای سریع همگام‌سازی requirements.txt

## ⚡ روش سریع (یک خط)

```bash
cd backend && source venv/bin/activate && python3 sync_requirements.py
```

یا با اسکریپت bash:

```bash
cd backend && ./sync-requirements.sh
```

## 📋 مراحل

1. **فعال‌سازی محیط مجازی:**
   ```bash
   cd backend
   source venv/bin/activate
   ```

2. **اجرای اسکریپت همگام‌سازی:**
   ```bash
   python3 sync_requirements.py
   ```

3. **بررسی تغییرات:**
   ```bash
   git diff requirements.txt
   ```

4. **Commit کردن (در صورت رضایت):**
   ```bash
   git add requirements.txt
   git commit -m "Update requirements.txt with actual installed versions"
   ```

## 🎯 چه اتفاقی می‌افتد؟

1. ✅ همه پکیج‌های `requirements.txt` نصب می‌شوند
2. ✅ نسخه‌های واقعاً نصب شده شناسایی می‌شوند
3. ✅ `requirements.txt` با نسخه‌های واقعی به‌روز می‌شود
4. 💾 یک backup خودکار از فایل قدیمی ایجاد می‌شود

## 🔍 قبل و بعد از همگام‌سازی

**قبل:**
```txt
fastapi==0.104.1
torch==2.9.0
```

**بعد (اگر نسخه واقعی متفاوت باشد):**
```txt
fastapi==0.104.1
torch==2.9.0  # اگر واقعاً 2.9.0 نصب شده باشد
```

## 💡 نکات مهم

- ⚠️ **فقط پکیج‌های اصلی به‌روز می‌شوند** (وابستگی‌های فرعی اضافه نمی‌شوند)
- 💾 **Backup خودکار** ایجاد می‌شود در `requirements.txt.backup`
- ✅ **در هاست**: وقتی `pip install -r requirements.txt` اجرا می‌شود، نسخه‌های درست نصب می‌شوند

## 🛠️ عیب‌یابی

### اگر اسکریپت خطا داد:

```bash
# بررسی Python
python3 --version  # باید 3.11+ باشد

# بررسی pip
pip --version

# نصب دستی پکیج‌های مشکل‌دار
pip install -r requirements.txt --force-reinstall
```

### اگر پکیجی نصب نشد:

```bash
# نصب دستی
pip install package-name==version

# سپس دوباره اجرای همگام‌سازی
python3 sync_requirements.py
```

